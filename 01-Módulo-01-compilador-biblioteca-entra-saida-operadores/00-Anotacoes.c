// Sintaxe: definição da sequência válida de lexemas da linguagem;
// Lexemas: são as unidades básicas da linguagem, como palavras-chave, identificadores,
// operadores, pontuação e outros elementos válidos.

// Semantica: Significado das sentenças e expressões, ou seja, o que elas fazem ou representam.



/* este é um comentário de mais de uma linha 
    repare que posso continuar escrevendo aqui sem me preocupar com as linhas*/


    // NEVTET : Uma variável possui seis propriedades
    /* - Nome : identificador que funciona como um apelido para o endereço do espaço reservado na memória;
       - Endereço : espaço de memória ocupado pela variável. Uma variável pode ocupar um ou mais endereços, mas usa apenas o primeiro como referência;
       - Valor : conteúdo armazenado no espaço de memória ocupado;
       - Tipo : determina a quantidade de memória ocupada pela variável, o formato de armazenamento do valor, a faixa de valores que podem ser armazenados e as operações que podem ser realizadas com a variável;
       - Escopo : região do programa onde a variável é reconhecida e pode ser acessada. Não pode haver duas variáveis com o mesmo nome no mesmo escopo;
       - Tempo de vida : período durante o qual a variável existe na memória. período entre a alocação (começou a ocupar um espaço na memória) até a liberação
        (deixa de ocupar um espaço na memória e foi apagada) da variável. 
    */

 /* Regra simples
    aspas simples ' ' → caractere
    aspas duplas " " → texto
    número → sem aspas
    
    Jeito fácil de memorizar
    %d → Decimal inteiro
    %f → Número com casas decimais
    %c → Caractere
    %s → string ou array de caracteres
    %i → i de inteiro (pode ser usado para int)
    %u → u de unsigned (é usado para unsigned int)
    %li → l de long (pode ser usado para long int e unsigned long int)
    %e → e de exponencial (pode ser usado para float, double e long double)
    %Le → L de long e e de exponencial (pode ser usado para long double)    
    */